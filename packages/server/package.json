{
  "name": "@af/server",
  "version": "0.0.1",
  "type": "module",
  "description": "Agent Fighter match server: matchmaking, WS relay, input ledger, headless result verification (ADR 0003).",
  "scripts": {
    "start": "tsx src/server.ts",
    "agent": "tsx src/agent-client.ts",
    "fleet": "tsx src/agent-fleet.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/ws": "^8.5.12"
  }
}
