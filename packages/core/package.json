{
  "name": "@af/core",
  "version": "0.0.1",
  "type": "module",
  "description": "Deterministic fighting game simulation core. No DOM, no Date, no Math.random.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "golden:bless": "tsx tools/bless-golden.ts",
    "build": "tsc -p tsconfig.json"
  }
}
